{
  "name": "client",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "spago build",
    "build-deps": "spago build --deps-only",
    "serve": "rm -rf .parcel/dev && spago build && parcel -d .parcel/dev --cache-dir .parcel/cache static/index.html --open",
    "bundle": "rm -rf dist .parcel/prod && mkdir -p .parcel && cp -r ./static/ .parcel/prod && spago bundle-app --to .parcel/prod/index.js && parcel build -d dist --cache-dir .parcel/cache --no-source-maps .parcel/prod/index.html"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "purescript": "^0.13.8",
    "purescript-psa": "^0.7.3",
    "spago": "^0.16.0"
  }
}
